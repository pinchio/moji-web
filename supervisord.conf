[supervisord]
nodaemon=true

[program:sshd]
command=/usr/sbin/sshd -D

[program:nginx]
command=/usr/sbin/nginx -c "/etc/nginx/nginx.conf"

[program:node]
command=cd /var/www && npm install --production && npm run start-dev
